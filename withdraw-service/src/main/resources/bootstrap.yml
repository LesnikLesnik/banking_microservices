spring:
  application:
    name: withdraw-service
  cloud:
    config:
      uri: ${SPRING_CONFIG_URI:http://localhost:8888}
      fail-fast: true
      password: admin
      username: user

server:
  port: 8083
