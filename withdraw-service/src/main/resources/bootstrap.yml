spring:
  application:
    name: withdraw-service
  cloud:
    config:
      uri: ${SPRING_CONFIG_URI:http://config-service:8888}
      fail-fast: true
      password: admin
      username: user

server:
  port: 8083
