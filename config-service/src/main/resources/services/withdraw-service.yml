spring:
  jpa:
    database: POSTGRESQL
    properties.hibernate.temp.use_jdbc_metadata_defaults: false
    hibernate:
      ddl-auto: create
    properties:
  datasource:
    platform: postgres
    url: jdbc:postgresql://${WITHDRAW_SERVICE_DATABASE_HOST:localhost}:5432/withdraw_service_db
    username: qwerty
    password: qwerty
    driver-class-name: org.postgresql.Driver

  rabbitmq:
    port: 5672
    host: localhost
    username: guest
    password: guest
    listener:
      direct:
        default-requeue-rejected: true

server:
  port: 8084
